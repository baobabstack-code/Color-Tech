// Frontend-compatible configuration
export const config = {
  // API configuration
  apiUrl: 'http://localhost:3000/api',
  
  // JWT configuration
  jwt: {
    expiresIn: '24h',
  },
};

export default config; 